class AppVersionConfig {
  // It handles all of these automatically:
  static const String currentVersion = 'v1.1';    // Becomes 1.0.0 in pubspec
// static const String currentVersion = 'v1.0.1'; // Becomes 1.0.1 in pubspec
}